# 작업명세서

## 작업 이력

---

### [2026-08-12] 5개 원전 기법 재구현 — 이름만 빌린 상태 정리

지표 계산(RSI/ATR)을 표준으로 고친 뒤 패턴 구현을 원전과 대조해보니, 5개 중 다수가
**이름만 빌려온 수준**이었다. 실제 투자에 쓸 거라 원전 규칙대로 다시 썼다.

**먼저 — 지표 계산 정정 (`screener.py`)**
- **RSI**: 단순이동평균(Cutler) → **Wilder 평활**. 60종목 실측 평균 4.06p, 최대 15.5p 차이.
  `50~70` 게이트 판정이 **60종목 중 15종목(25%)에서 뒤집혔다**
- **ATR**: 단순이동평균 → **Wilder 평활**. 평균 4.5%, 최대 15.2% 차이.
  손절가가 주가 대비 평균 0.46%p, 최대 1.72%p 어긋났음
- 볼린저 σ(ddof)는 `bb_squeeze`가 자기 분위수와 비교하는 구조라 배율이 상쇄 → 판정 영향 0/60건. 유지
- MACD·이동평균·OBV는 정의상 표준 (`ewm(span, adjust=False)`가 표준 EMA 재귀식)

**추가 — `screener/patterns.py`** (차트 구조 탐지)
- Darvas 박스: 신고가 3봉 미경신 → 천장, 이후 최저가 3봉 유지 → 바닥
- VCP 연속 수축: 스윙 고→저를 한 번의 수축으로 보고 점점 얕아지는지 판정
- 거래범위: 끝에서부터 넓히며 폭 제한을 넘는 순간 멈춰 실제 횡보 구간만 인식
- Wyckoff Spring/SOS/매도소진
- IBD `rs_strength` (0.4×ROC63 + 0.2×ROC126 + 0.2×ROC189 + 0.2×ROC252)

**패턴별 재구현**

| 기법 | 이전 (틀린 부분) | 현재 |
|------|------------------|------|
| CAN SLIM | L = 코스피 대비 +5% — **순위가 아니라 절대값** | RS Rating 백분위 70↑, M(시장 방향) 추가, C는 사후 가산 |
| VCP | 볼린저 수축 — **연속성을 안 봄** | Trend Template 8개 + 수축이 점점 얕아지는지 |
| Stage 2 | MA120 위 — **'전환'이 아니라 '상태'** | 30주선(150일) + 베이스 저항 돌파 + 거래량 2배 |
| Darvas | **박스가 아예 없었음** | 박스 천장/바닥 실제 탐지, 손절 = 박스 바닥 |
| Wyckoff | OBV 신고점 — **OBV는 Wyckoff 사후 지표** | 거래범위 + Spring/SOS + 매도 소진 |

**RS Rating 2단계 구조** — 백분위라 종목 하나로는 못 구한다
- `calc_signals_from_df` → `rs_strength` (종목별) → `attach_rs_rating` → 유니버스 백분위 → 스코어링
- 라이브와 재현이 **같은 모집단(거래 가능 종목)** 사용. 어긋나면 `verify`가 못 잡음

**구조 탐지 검증에서 잡은 버그**
- 첫 구현은 구간 최고·최저를 그대로 박스로 잡아 **폭 140%짜리 "박스"** 가 나왔다(SK하이닉스 124만~299만원)
- 폭 상한(25%)과 바닥 이탈 시 무효 규칙 추가, 오른쪽부터 훑어 최근 박스 사용
- 베이스도 고정 120봉 → 폭 30% 이내로 횡보한 만큼만 인식 (278% → 29%)
- 검출률 실측: 박스 2.5%(중앙값 폭 12.4%), VCP 수축 16.3%

**공통 추출 재정의**
- VCP가 Trend Template(상승추세)을, Wyckoff가 거래범위(횡보)를 요구해 **둘이 배타적**이 됨
  → `common_accum`을 Wyckoff 단독으로, VCP는 돌파형(`common_trend`)으로 이동
- 구성 패턴이 1개인 카테고리에 "2개 이상"을 요구하면 영원히 비므로 `min(2, len)`으로

**설정 변경**
- `lookback_bars` 300 → 400 (MA200 기울기 221봉 + RS 4분기 253봉 수용)
- `data_days` 480 → 600, `MIN_BARS` 150 → 253

**효과 측정 (KR, 66,472행 / 2,010종목 / 67스캔일)**
- **Stage 2 동점률 96% → 5%** — 이전엔 점수의 81%가 만점이라 상위 30이 통째로 동점,
  순위가 사실상 임의였다. 이게 재구현이 작동했다는 가장 확실한 증거
- 신호 발생률: canslim 1.7% / vcp 1.4% / darvas 1.7% / stage2 3.6% / wyckoff 5.2%
  (원전 기법은 원래 희소하다 — 화면에 30종목이 아니라 몇 종목만 뜰 수 있음)
- 20일·상위30 선정력: stage2 **+2.20%**(승률 52%), wyckoff +0.38%, p3 +0.12%, 나머지 음수
- 패턴 간 중복(자카드) 최대 30.5%(canslim∩darvas — 둘 다 신고가+거래량 요구)
- ⚠️ 여전히 단일 구간(2025-04~2026-08) 결과이고 보유 구간이 겹쳐 63일이 독립 표본이 아님

**⚠️ 원전 대비 남은 차이 (문서에 조건별로 명시)**
- CAN SLIM **A**(3년 연속 이익): DART가 약 2년치만 제공 → 미구현
- CAN SLIM **I**(기관 매수): 외인·기관 순매수 미연동 → 미구현
- CAN SLIM **C**: 유니버스 전체 재무를 못 받아 선정 게이트가 아닌 **사후 가산(15점)**
- Wyckoff **국면 판정**: SC/AR/ST 식별은 거래량-스프레드 재량 해석 → 미구현
- Stage 2 **주봉**: 원전은 주봉 30주선, 여기서는 일봉 150선 (근사)
- VCP **스윙 정의**: 좌우 5봉 기준은 원전에 수치가 없어 임의 선택

**변경 파일**: `screener/patterns.py`(신규), `screener/screener.py`, `screener/market_config.py`,
`screener/main.py`, `screener/replay.py`, `screener/backtest.py`,
`dashboard/lib/types.ts`, `dashboard/lib/patternGuide.ts`, `패턴기법정리.md`, `Architecture.md`

---

### [2026-08-12] 실전 성적표 — 일일 스크리너에서 자동 계산

`replay.py`를 수동 워크플로로 만들었더니 "N일 전 이 패턴 종목, 지금 몇 %"를 보려면
매번 Actions를 돌려야 했다. 과한 설계였다 — 그 질문에 필요한 두 가지가 이미 매일 들어온다.
  1. 과거에 뜬 종목 목록 → `screener_results`에 하루 2회 저장 중 (2026-05-11~)
  2. 그 종목들 현재가 → 스크리닝이 유니버스 전체 시세를 이미 받음

**추가 — `screener/tracker.py`**
- 과거 문서를 읽어 그때 표시됐던 price와 오늘 price를 비교. 추가 조회 없음
- 재구성이 아니라 **실제로 화면에 떴던 기록**이라 생존편향·룩어헤드가 없다
- 같은 날 두 번(08:30/18:00) 실행되므로 날짜당 마지막 문서만 사용
- 오늘 유니버스에서 사라진 종목(폐지·거래정지·유동성 미달)은 `gone`으로 표시하고 평균에서 제외
- 과거 문서의 `backtest` 블록이 용량 대부분이라 `select()`로 필요한 필드만 읽음
- `screener.run()`이 `(output, prices)` 튜플을 반환하도록 변경 — 유니버스 전체 시세 전달용

**수정 — 업로드 순서 (`main.py`)**
- 기존: 스크리닝 → 백테스트 → 업로드. 백테스트가 90분 잡 타임아웃에 걸리면 프로세스가
  통째로 죽어 **그날 스크리닝 결과까지 같이 날아갔다**
- 변경: 스크리닝 → 업로드 → 성적표 업로드 → 백테스트 → `attach_backtest()`로 병합
- 오늘 볼 종목이, 매일 돌 필요 없는 백테스트에 인질로 잡히지 않게 함

**추가 — 대시보드 `/track`**
- 진입일 슬라이더 + 빠른 이동(5/10/20/30/60일 전) + 패턴 선택 + 종목 검색
- 종목별 그날 가격 / 현재가 / 수익률, 상단에 평균·중앙값·최고·최저
- 하루 2회 자동 갱신이라 "현재가"가 항상 최신
- `/replay/picks`는 같은 질문에 화면이 둘이 되어 제거 — 3년치 통계는 `/replay`가 담당

**검증** — 가짜 Firestore로 tracker 동작 확인: 같은 날 중복 제거, 폐지 종목 제외,
시세 없는 시장 제외, 평균 계산 일치

**변경 파일**: `screener/tracker.py`(신규), `screener/main.py`, `screener/screener.py`,
`screener/firebase_upload.py`, `dashboard/app/track/page.tsx`(신규),
`dashboard/app/replay/picks/`(삭제), `dashboard/lib/types.ts`, `dashboard/lib/fetcher.ts`,
`dashboard/components/Header.tsx`, `Architecture.md`

---

### [2026-08-12] 과거 재현 — "이 패턴 리스트를 샀으면 얼마 벌었나"

기존 `backtest.py`는 "신호 뜬 종목 전부 매수"를 재는데, 실제 사용 행동은 화면에 뜬
상위 30종목 중에서 고르는 것이다. 리스트 단위로 측정하는 `replay.py`를 추가했다.

**추가 — `screener/replay.py`**
- 2단계 구조: `build`(신호 패널 parquet 적재, 느림 1회) / `eval`(조건 교체, 즉시)
  - 보유일·상위N·임계값은 신호 계산 뒤에 붙는 값이라 분리. 매번 재스캔하면 화면에서 못 씀
- 생존편향 보정: `KRX-DELISTING`으로 폐지 종목 포함 (KR 2,552 → 2,655종목)
  - 보유 중 폐지 시 마지막 체결가 청산. 행을 버리면 편향이 되돌아옴
- 시총 필터 미적용 — 과거 시총을 오늘 값으로 거르면 미래 정보가 샘. 유동성만 시점별 판정
- 진입을 **신호 다음날 시가**로 변경 (당일 종가 진입은 거래량 급등일 갭상승을 공짜로 먹음)
- `verify` 서브커맨드: 조회 봉 수(300 vs 전체)를 달리해 라이브/재현 신호 동일성 검증
  - 처음엔 조회 '시작일'을 달리했는데, `calc_signals_from_df`가 내부에서 자르므로 두 프레임이
    같은 300봉으로 수렴해 **항상 통과하는 무의미한 검사**였음. 잘라내는 봉 수를 벌리도록 수정
  - 옛 OBV 공식으로 되돌리면 실제로 실패하는지 확인 (수정본 20/20 통과, 옛 공식 1종목 검출)
  - 불리언·정수는 정확 비교, 실수는 상대오차 1e-4 (MACD EWM 초기화 잔차가 반올림에서 드러남)
- `picks` 서브커맨드: 특정 날짜 리스트의 **종목별** 수익률 (집계가 아니라 개별 내역)
- CLI: `build` / `eval` / `picks` / `verify` / `publish`, `--hold` `--top` `--threshold` `--no-stop`

**수정 — 신호가 조회 구간에 따라 달라지던 문제 (`screener.py`)**
- `obv_new_high`가 `obv[-1] >= obv[-60:].max() * 0.98`인데 OBV는 `cumsum`이라 절대값이
  조회 시작점에 의존. `× 0.98`이 기준선을 같이 움직여 라이브(290봉)와 백테스트(253~700봉)가
  **서로 다른 지표를 계산**하고 있었음 (표본 8종목 중 1종목에서 플래그 반전 확인)
  - 60일 변동폭 대비 상대 위치로 변경 → 구간 이동에 불변
- `lookback_bars` 도입 (KR/US 300봉, 코인 420봉) — 라이브·백테스트·재현이 같은 봉 수 사용
  - `data_days` 420 → 480 (휴장일 포함해도 300봉 확보)
- ⚠️ 라이브 선정 종목이 바뀜 (의도된 수정)

**수정 — 동점 종목의 화면 순서가 실행마다 바뀌던 문제 (`screener.py`)**
- `as_completed`는 완료 순서라 결과 DataFrame 행 순서가 비결정적.
  `stage2`는 점수의 **81%가 100점 만점**이라 상위 30이 통째로 동점 → 목록 자체가 매번 달라짐
  - 결과 정렬을 `ticker` 기준으로 고정
- 재현에서는 고정 시드 난수로 타이브레이크. 행 순서로 두면 적재 순서(≈종목코드)가 순위가 되어
  **없는 선정력이 생김** — `stage2`가 +2.46%로 보이던 게 랜덤 타이브레이크에서 -0.86%로 소멸

**수정 — 거래정지일 0원 청산 (`screener.sanitize_ohlc`)**
- FinanceDataReader는 거래정지일에 시/고/저를 **0**으로 주고 종가만 직전 값으로 유지
  - `저가 0 <= 손절가`로 잡혀 0원 청산 → **-100% 수익률**. 20일 기준 214건(90종목),
    카카오페이·GS리테일 등 정상 종목 포함
  - ATR(고가-저가)과 52주 저점도 같이 오염 → 라이브(`_get_signals`)와 `backtest.py`에도 적용
    (`backtest.py`가 누락돼 있었음 — 하루 2회 크론이 오염된 PF를 화면에 올리고 있었다)
- 정정 후 패널 평균: 20일 -1.22% → -0.89%, 60일 +0.41% → +1.57%

**측정 지표 — 지수가 아니라 유니버스 평균을 기준선으로**
- 재현 구간(2024-11 ~ 2026-08) KOSPI +149.6%. 지수는 시총가중이라 대형주 몇 개가 끌어올린
  장에서 동일가중 30종목은 구조적으로 뒤짐 → 종목 선정력이 아니라 가중방식 차이를 재게 됨
- 그날 유동성 통과 전 종목의 동일가중 평균을 기준선으로 추가(`선정력 = 리스트 − 유니버스`)
- `tie_ratio`(상위 N이 전부 동점인 날의 비율) 노출 — 높으면 순위·RankIC 해석 불가

**추가 — "지금까지 보유" 기준 (`ret_now`)**
- 고정 보유기간(20거래일 뒤 매도)과 **"안 팔고 현재가"** 는 다른 질문이라 둘 다 제공
  - `now`: 그날 사서 오늘까지 — 실제 손익 확인용. 손절 미적용(터치 여부만 표시)
  - `5/20/60`: N거래일 뒤 매도 + ATR 손절 — 보유일이 같아야 패턴 간 비교가 성립
- 폐지 종목은 시계열이 끝난 날까지만 재고, 벤치마크도 같은 날로 맞춰 비교 성립시킴

**추가 — 대시보드 `/replay/picks` (종목별 성과)**
- 날짜 슬라이더(최근 40스캔일) + 패턴 선택 + 종목 검색 → 종목별 매수가·현재가·수익률
- 기본 날짜는 **20거래일 전에 가장 가까운 스캔일** (가장 흔한 질문)
- 상단에 리스트 평균 / 유니버스 평균 / 선정력 / 지수 대비 4개 카드
- 아직 보유기간이 안 지난 조합은 "진행 중"으로 표기
- Firestore `replay_picks/{market}_{date}` — 날짜당 문서 분리(약 45KB). 전 기간을 한 문서에
  담으면 1MB 한도 초과. 화면은 고른 날짜 하나만 읽음

**추가 — 대시보드 `/replay` (패턴별 집계)**
- 보유일(5/20/60) × 상위N(10/20/30) × 손절 유무 조합 198개를 미리 계산해 `replay_results/{market}`에 저장
- 패턴별 리스트/유니버스/선정력/승률/vs지수/RankIC/동점률 + 순위 구간별 초과수익

**추가 — `.github/workflows/replay.yml`** (수동 실행, 300분)
- 일일 스크리너(90분)와 분리. parity 검증 → 패널 적재 → 아티팩트 업로드 → Firebase 순
- `verify`에 표본 하한(`max(5, n//2)`) — 조회가 전부 실패해도 "불일치 0"으로 통과해
  검증 안 된 경로에서 몇 시간짜리 적재가 도는 것을 막음
- 패널을 아티팩트로 남김 — 업로드 실패 시 재적재 대신 재시도로 끝남

**측정 결과 (KR, 87스캔일, 2,221종목, 88,794행)**
- 20일·상위30: 유니버스 평균 대비 선정력이 뚜렷한 패턴 **없음**.
  p3 +0.33%, wyckoff -0.01%가 최선이고 나머지는 음수 (vcp -1.91%가 최하)
- 60일·상위30: canslim +0.98%, common_trend +0.88%, p3 +0.24%로 소폭 양수.
  p2 -3.32%, vcp -2.55%는 뚜렷한 음수
- 상위 10종목으로 좁히면 **대부분 더 나빠짐** (canslim 20일 -0.34% → -4.23%).
  순위 구간별로도 상위권이 하위권보다 나쁜 패턴이 다수 — 점수 정렬에 정보가 없음
- ATR 손절(-1.5) 20일 도달률 59% — 손절 적용이 성과를 깎는 구간이 있음 (`--no-stop`으로 비교)
- ⚠️ 재현 구간은 KOSPI +149.6%의 단일 대형주 랠리 국면. 8패턴 전부 롱온리 추세추종이라
  이 한 구간 결과로 "패턴이 작동하지 않는다"고 결론낼 수 없음. `--days`로 구간을 바꿔 재확인 필요
- ⚠️ 보유 구간이 겹쳐 83일이 독립 표본이 아님 — 유의성 판단 금물

**남은 과제**
- `stage2` 점수 81%가 만점 — 임계값·배점이 변별력을 못 만듦. 측정 도구가 생겼으니 다음 단계

**변경 파일**: `screener/replay.py`(신규), `screener/screener.py`, `screener/market_config.py`,
`screener/firebase_upload.py`, `.github/workflows/replay.yml`(신규),
`screener/backtest.py`, `dashboard/app/replay/page.tsx`(신규),
`dashboard/app/replay/picks/page.tsx`(신규), `dashboard/app/backtest/page.tsx`,
`dashboard/lib/types.ts`, `dashboard/lib/fetcher.ts`, `dashboard/components/Header.tsx`,
`.gitignore`, `Architecture.md`

---

### [2026-08-03] NaN 저장 버그 + 탭 레이아웃 깨짐 수정
- **NaN 버그**: pandas가 결측을 `None` 대신 `NaN`으로 채워 Firestore에 그대로 저장됨.
  프론트의 `!= null` 검사는 NaN을 통과시켜 화면에 `NaN%`으로 표시됨
  - `firebase_upload._to_native()`에서 NaN → None 정규화 (뿌리 수정)
  - 이미 저장된 데이터 방어용으로 `lib/utils.ts`에 `isNum()` 추가, 렌더 가드 전면 교체
- **탭 레이아웃**: `TabsList` 기본 스타일이 가로 한 줄 탭 전제라 3그룹 레이아웃과 충돌
  - 주범은 `group-data-horizontal/tabs:h-8`(높이 32px 고정). variant 접두사가 붙어 있어
    일반 `h-auto`로는 덮이지 않음 → 같은 접두사로 재정의
  - 트리거의 `flex-1`, `h-[calc(100%-1px)]`도 `flex-none h-auto`로 교체
  - **부수 발견**: 기존 코드가 `data-[state=active]`를 썼으나 이 컴포넌트는 Base UI라
    `data-active`가 맞음 — 활성 탭 하이라이트가 원래부터 적용되지 않고 있었음
- **검증**: 렌더된 HTML에서 `h-8`·`inline-flex`·`w-fit` 제거 확인, tsc 통과
- **변경 파일**: `screener/firebase_upload.py`, `dashboard/lib/utils.ts`,
  `dashboard/app/page.tsx`, `dashboard/components/{StockTable,StockDetailModal,PeerCompare}.tsx`

---

### [2026-08-03] 스크리너 화면 정보구조 개편 (가독성)
- **문제**: 패턴 11개가 한 줄에 평평하게 나열돼 무엇부터 볼지 알 수 없었고,
  펀더멘털 지표 추가로 테이블 컬럼이 최대 15개까지 늘어나 스캔이 불가능
- **패턴 3그룹 위계**:
  - ★ 공통 신호(3) — 여러 기법이 동시에 가리킴 · 신뢰도 최상
  - 전설 기법(5) — 검증된 트레이더 방법론
  - 내 패턴(3) — 한국 시장 특화 자체 개발
  - 각 탭에 종목 수를 함께 표기해 클릭 전에 규모를 알 수 있게 함
- **테이블 컬럼 그룹 토글**: 기본은 종목/현재가/3M/패턴신호/점수만.
  `기술지표`(RSI·거래량·거래대금), `펀더멘털`(PER·PBR·PSR·ROE·재고YoY)은 토글로 확장
  - 펀더멘털 토글은 값이 있는 시장(KR)에서만 노출
- **검증**: dev 서버 렌더 확인 — 3그룹 라벨 정상, role=tablist 1 / role=tab 11 / 중첩 button 0건,
  런타임 에러 0. 프로덕션 빌드 통과
- **보류**: 업황 전용 라우트(`/cycle`)는 산업 집계 데이터가 없어 종목별 정보 재탕이 되므로 보류.
  fundamentals 컬렉션이 채워지고 업종 집계가 생긴 뒤 재검토
- **변경 파일**: `dashboard/app/page.tsx`, `dashboard/components/StockTable.tsx`

---

### [2026-08-03] CAPEX 사이클 지표
- **내용**: 반도체 사이클의 **선행 신호**인 설비투자 추적. 재고보다 앞선 지표
- **왜 CAPEX인가**: 반도체는 공급이 사이클을 결정 — 감산/투자축소가 가격 반등에 선행.
  재고·실적은 후행이라 확인 후 진입하면 늦음(주가는 실적보다 6~12개월 선행)
- **현금흐름표는 누적값** → 손익(단독분기)과 반대이므로 전용 차감 경로(`_single_cum_flow`)
  - 단독분기 = 당분기누적 − 직전분기누적. 직전 분기 없으면 계산 불가(None), 누적값 폴백 금지
  - 이 때문에 히스토리를 8 → **10분기**로 확대 (가장 오래된 1개가 차감 기준으로 소모)
- **추가 지표**: TTM CAPEX/영업CF, CAPEX 강도(매출 대비), **CAPEX 추세**(최근 4분기 vs 직전 4분기), FCF·FCF마진
- **검증**: 삼성 2025 단독 CAPEX 합 47.4조 / 하이닉스 27.5조 — DART 연간 공시값과 일치
  - 하이닉스 CAPEX 추세 +51%(증설), 삼성 +4.8%
  - 현대차 영업CF 음수(캐피탈 연결) — FCF 조건을 필터로 쓰면 금융 계열 제외되므로 주의
- **변경 파일**: `screener/dart_fetcher.py`

---

### [2026-08-03] 재고 사이클 차트 + 동종업계 비교
- **내용**: 저장된 분기 재무를 활용한 시각화 2종
- **검증으로 바뀐 설계 2가지**:
  - **현금흐름표(CF)는 누적값** — 삼성 2025 영업CF Q1 16.6조 → Q2 33.9조 → Q3 56.5조 단조증가 확인.
    손익(IS)은 단일 분기인데 CF만 반대 → 향후 F-Score 등 CF 지표는 차감 필요
  - **FDR `Dept`가 943종목 전부 NaN** → 기존 `sector`는 사실상 빈 값이라 업종 비교 불가.
    DART `company()`의 `induty_code`(KSIC)로 대체 — HD현대일렉 281 / LS ELECTRIC 28121 → 3자리로 매칭
- **재고 사이클 차트**: 8분기 재고·매출 추이 + 사이클 국면 판정
  - 재고↓+매출↑ = 업황 개선 / 재고↑+매출↓ = 재고 부담 / 나머지 = 확장·조정
  - 색상은 dataviz 검증 팔레트(다크 서피스 #0f1117 전 항목 PASS), 단일 축(이중축 금지)
- **동종업계 비교**: KSIC 3자리 매칭, 부족하면 2자리(대분류)로 확장. PER/PBR/ROE/영업이익률/재고YoY
  - 비교 모집단은 "스크리닝 통과 종목"이라 UI에 명시 (전체 상장사 아님)
- **⚠️ 선행 조건**: Firestore 규칙에 `fundamentals` 읽기 허용 필요 (현재 permission-denied 확인됨)
- **변경 파일**:
  - `screener/dart_fetcher.py` — `induty_code` 수집
  - `dashboard/lib/fetcher.ts` — `fetchFundamentals()` 추가
  - `dashboard/lib/types.ts` — `Fundamentals`/`QuarterRecord` 타입, `induty` 필드
  - `dashboard/components/InventoryCycleChart.tsx` — 신규
  - `dashboard/components/PeerCompare.tsx` — 신규
  - `dashboard/components/StockDetailModal.tsx` — 두 컴포넌트 배치 + 스크롤
  - `dashboard/components/StockTable.tsx`, `dashboard/app/page.tsx` — universe 전달

---

### [2026-08-03] DART 재무 히스토리 + 밸류에이션 지표 + 패턴 설명 UI
- **내용**: 펀더멘털 분석 서비스 1단계 — 분기 재무 수집·저장, PER/PBR/PSR 표기, 패턴 설명 모달
- **DART 수집 재설계** (프로브로 실검증):
  - `finstate`(주요계정) → `finstate_all`(전체 재무제표)로 교체 → 재고자산·매출채권·현금 확보
  - 손익계정을 `account_id`(IFRS 코드)로 매칭 — 회사마다 계정명 다름(당기/분기순이익)
  - 손익계산서를 `IS`/`CIS`(포괄손익) 둘 다 탐색 — 하이닉스 등은 CIS로만 공시
  - 분기보고서 `thstrm_amount`는 당기 3개월 단독값 확인 → 차감 불필요(Q4=연간−3Q누적)
  - 연결(CFS) 우선, 없으면 별도(OFS) 폴백
  - 최근 8분기 히스토리 → `fundamentals/{ticker}` 별도 컬렉션 (screener_results 1MB 한도 회피)
  - 파생지표: TTM 매출/영업익/순익, ROE/ROA/영업이익률/순이익률/부채비율, 재고 QoQ/YoY/회전율
- **밸류에이션**: `Marcap`(StockListing) ÷ TTM 재무값 = PER/PBR/PSR (주가는 DART에 없어 시총과 결합)
  - 삼성/하이닉스/현대차 실데이터 검증 — 현대차 ROE 7.2%·PBR 0.42 등 실제값과 일치
  - `screener/screener.py` line 36 한계("PER/PBR 미포함") 해소
- **대시보드**: 종목 리스트에 PER/PBR/PSR/ROE 컬럼, 상세 모달에 펀더멘털·재고 사이클 섹션
- **패턴 설명 UI**: 각 패턴에 `?` 버튼 → 설명 모달(요약/조건표/점수배분/적합기간). 원문 하드코딩
- **보안**: 루트 `.gitignore` 신규 — `.env`·`__pycache__`·`.serena` 등 커밋 차단
- **변경 파일**:
  - `screener/dart_fetcher.py` — 전면 재작성(finstate_all + derive + add_valuation)
  - `screener/screener.py` — `marcap` 배관(get_universe → BASE_COLS)
  - `screener/main.py` — fetch_batch 반환 변경 + 밸류에이션 + fundamentals 저장
  - `screener/firebase_upload.py` — `save_fundamentals()` 추가
  - `dashboard/lib/types.ts` — 펀더멘털 필드 추가
  - `dashboard/lib/patternGuide.ts` — 패턴 설명 데이터 (신규)
  - `dashboard/components/PatternGuideModal.tsx` — 설명 모달 (신규)
  - `dashboard/components/StockTable.tsx` — PER/PBR/PSR/ROE 컬럼
  - `dashboard/components/StockDetailModal.tsx` — 펀더멘털·재고 섹션
  - `dashboard/app/page.tsx` — 패턴 `?` 버튼
  - `.gitignore` — 신규
- **미적용(후속)**: 증분 수집(현재 매 실행 8분기 재수집), 재고 사이클 추세 차트, 기업 비교/공시/AI 리포트

---

### [2026-05-11] 프로젝트 초기화
- **내용**: 프로젝트 구조 및 문서 초안 생성
- **생성 파일**:
  - `CLAUDE.md` — AI 행동 지침
  - `작업명세서.md` — 작업 이력 (현재 파일)
  - `Architecture.md` — 시스템 구조

---

### [2026-05-11] Python 스크리너 + 자동화 구축
- **내용**: 스크리너 로직, Firebase 업로드, GitHub Actions 워크플로 작성
- **생성 파일**:
  - `screener/screener.py` — 스크리너 (초기 pykrx 기반)
  - `screener/firebase_upload.py` — Firestore 업로드
  - `screener/main.py` — 진입점
  - `screener/requirements.txt` — 의존성
  - `.github/workflows/screener.yml` — 하루 2회 자동 실행 (08:30, 16:00 KST)

---

### [2026-05-13] pykrx → FinanceDataReader 마이그레이션
- **내용**: pykrx KRX 로그인 문제(사이트 클라이언트 측 암호화)로 인해 데이터 소스 교체
- **원인 분석**:
  - data.krx.co.kr이 비밀번호를 클라이언트 측에서 암호화하여 전송
  - pykrx가 평문 비밀번호를 보내므로 구조적으로 로그인 불가
  - `test_login.py`로 직접 디버깅하여 확인
- **변경 파일**:
  - `screener/requirements.txt` — pykrx → finance-datareader
  - `screener/screener.py` — FinanceDataReader 기반으로 재작성
- **한계**: FinanceDataReader StockListing에 PER/PBR 미포함 → 기술적 분석 중심으로 전환

---

### [2026-05-13] Firebase Firestore 연동 완료
- **내용**: GitHub Actions → Firestore 업로드 파이프라인 정상화
- **해결한 문제들**:
  - Firestore 데이터베이스 미생성 → (default) DB 생성
  - 서비스 계정 IAM 권한 부재 → Google Cloud Console에서 `Cloud Datastore Owner` 역할 추가
- **결과**: Actions 실행 시 `screener_results` 컬렉션에 정상 저장 확인

---

### [2026-05-13] 3패턴 퀀트 스크리너 고도화
- **내용**: 단순 모멘텀에서 차트 기술적 분석 기반 3패턴 스크리너로 완전 재설계
- **추가된 지표**:
  - MA5/20/60/120 정배열 분석
  - MACD (12/26/9) — 골든크로스, 히스토그램 방향
  - RSI (14) — 추세 강도 및 과열 판단
  - 볼린저 밴드 수축 (Squeeze)
  - OBV — 매집/분산 판단
  - 피보나치 되돌림 (38.2% ~ 61.8%)
  - 눌림목 감지 (고점 대비 -3% ~ -15%)
- **3가지 패턴 스코어링**:
  - `P1` 정배열 퍼지기 직전 + 매집: 볼린저 수축 + OBV 상승 + 이평선 막 돌파
  - `P2` 5일선 추세 + 거래량 터짐: 완전 정배열 + MACD > 0 + RSI 50~70 + 거래량 2배↑
  - `P3` 눌림목: 피보나치 구간 + MACD 히스토그램 바닥 반등 + MA20 지지
- **변경 파일**:
  - `screener/screener.py` — 3패턴 스코어링 시스템
  - `screener/main.py` — 패턴별 결과 출력
  - `screener/firebase_upload.py` — p1/p2/p3 배열로 저장
- **Firebase 저장 구조**: `screener_results/{date}_{run_type}` → `{p1: [...], p2: [...], p3: [...]}`

---

### [2026-05-13] Next.js 대시보드 구축
- **내용**: Firebase 데이터를 읽어 표시하는 웹 대시보드 구축
- **기술 스택**: Next.js 15 + Tailwind CSS + shadcn/ui + Recharts + TanStack Table + SWR + Framer Motion
- **생성 파일**: `dashboard/` 전체 (app, components, lib)
- **주요 기능**: 패턴별 탭, 종목 테이블(정렬/필터), 상세 모달(레이더 차트), 스코어 바

---

### [2026-05-13] 전설적 트레이더 기반 8패턴 스크리너 완성
- **내용**: 단순 3패턴에서 전설적 트레이더 방법론 기반 8패턴 + 3개 공통 추출로 확장
- **추가 패턴**:
  - `canslim` — O'Neil CAN SLIM (52주 신고가 + 거래량 + 상대강도)
  - `vcp` — Minervini VCP (변동성 수축 패턴)
  - `stage2` — Weinstein Stage 2 (MA120 위 안정 추세)
  - `wyckoff` — Wyckoff 매집 (OBV 신고점 스마트머니)
  - `darvas` — Darvas Box (52주 신고가 박스 돌파)
- **공통 추출 2종**:
  - `common_trend` — Stage2 + CAN SLIM + Darvas 중 2개+ (신고가형 추세)
  - `common_accum` — Wyckoff + VCP 둘 다 (매집/수축형)
  - `common_all` — 내 P1+P2+P3 중 2개+ (참고용)
- **추가 지표**: 52주 위치(`pos_52w`), 상대강도(`rs`), ATR 손절가(`stop_swing`, `stop_lt`), 섹터 분류

---

### [2026-05-13] DART API 연동 — EPS/매출 성장률
- **내용**: 금감원 DART API로 분기 EPS·매출 성장률 추가 (O'Neil CAN SLIM C 조건)
- **생성 파일**: `screener/dart_fetcher.py`
- **추가 필드**: `eps_yoy`, `rev_yoy`, `canslim_c` (EPS 25%↑ 여부)
- **동작 방식**: 기술적 스크리닝 완료 후 추출된 종목들만 DART API 호출 (효율화)

---

### [2026-05-13] 백테스트 모듈 구축
- **내용**: 3개 공통 카테고리(common_trend, common_accum, common_all) 대상 슬라이딩 윈도우 백테스트
- **생성 파일**: `screener/backtest.py`
- **방식**: 500일 히스토리를 매주(5거래일) 슬라이딩 → 신호 발생 시점 탐지 → 20일/60일 수익률 계산
- **통계 항목**: 신호 수, 승률, 평균 수익률, 평균 수익/손실, 손익비(Profit Factor), 베스트 트레이드 Top3
- **저장**: Firebase `screener_results/{date}` 문서의 `backtest` 필드
- **버그 수정**: 초기 270일 → 500일로 확장 (신호 스캔 구간 부족 문제 해결)

---

### [2026-05-13] 백테스트 대시보드 페이지 구축
- **내용**: `/backtest` 전용 페이지 + 상단 네비게이션(대시보드/백테스트) 분리
- **생성 파일**:
  - `dashboard/app/backtest/page.tsx` — 백테스트 결과 페이지
  - `dashboard/components/Header.tsx` — 공통 네비게이션 헤더
- **주요 기능**:
  - 3개 카테고리 비교 바 차트 (승률, 평균수익)
  - 카테고리별 통계 카드 + 레이더 차트
  - 베스트 트레이드 Top3 표시

---

### [2026-05-13] 멀티마켓 지원 — 미장(S&P500) + 코인(Top25)

- **내용**: 한국 전용에서 3개 시장 동시 스크리닝으로 확장
- **신규 파일**: `screener/market_config.py` — 시장별 설정 집중 관리
- **지원 시장**:
  - 🇰🇷 **한국** (KOSPI + KOSDAQ) — 기존
  - 🇺🇸 **미국** (S&P500 500종목) — FinanceDataReader, 벤치마크 SPY
  - ₿ **코인** (Top 25 코인) — BTC/ETH/SOL 등, 벤치마크 BTC
- **시장별 임계값 조정**:

| 항목 | 한국 | 미국 | 코인 |
|------|------|------|------|
| 거래량 기준 | 2배 | 2배 | **3배** |
| 모멘텀 기준 | 5%+ | 5%+ | **10%+** |
| 눌림목/VCP | -3~-15% | -3~-15% | **-5~-30%** |
| RSI 건강 | 50~70 | 50~70 | **45~75** |
| 점수 임계값 | 40점 | 40점 | **35점** |

- **Firebase 저장**: `kr_p1`, `us_common_trend`, `crypto_wyckoff` 형태로 마켓 prefix 분리
- **대시보드**: 🇰🇷 🇺🇸 ₿ 마켓 선택 버튼 추가

---

### [2026-08-02] 신뢰도 개선 — 백테스트 편향 제거 + 스크리닝 필터 강화

- **내용**: 로직 검토 결과 발견된 방법론 결함 일괄 수정. 백테스트 결과가 구조적으로
  부풀려지던 문제와, 스크리너 임계값이 사실상 필터 역할을 못 하던 문제를 함께 해결.

**A. 백테스트 (치명적)**

| 문제 | 수정 |
|------|------|
| 오늘 선정된 종목만 과거 스캔 → 선택 편향으로 승률 부풀려짐 | 유니버스 무작위 표본 150종목(seed 고정)을 스캔하도록 변경 |
| 수수료·세금·슬리피지 미반영 | 시장별 거래비용 차감 (KR 왕복 약 0.41%) |
| ATR 손절을 계산만 하고 백테스트에 미적용 | 보유 중 손절 도달 시 조기 청산, 갭하락은 시가 체결 |
| `market_return=0.0` 하드코딩 → RS가 절대 모멘텀이 됨 | 스캔 시점 벤치마크 3개월 수익률을 전달 |
| 5일 간격 스캔 + 20일 보유 → 신호 중복 과다 | 스캔 간격 10거래일로 확대 |
| `profit_factor`가 실제로는 손익비(payoff ratio) | 총이익/총손실로 정정, 손익비는 별도 필드로 분리 |
| 카테고리마다 신호를 3번 중복 계산 | 스캔 1회로 3개 카테고리 동시 평가 |

**B. 스크리닝 필터**

| 문제 | 수정 |
|------|------|
| 임계값 40이 조건 2개로 통과 (Stage2 = MA120 위 + 우상향 = 50점) | 패턴별 필수조건 게이트(`REQUIRED_FNS`) 도입 + 임계값 60으로 상향 |
| 거래정지 종목이 VCP 만점권 (가격 고정 → 볼린저 수축 항상 참) | 거래량 0일수·가격 표준편차 0 체크로 배제 |
| 유동성 필터 없음 (시총만 봄) → 못 사는 종목이 상위 랭크 | 20일 평균 거래대금 기준 추가 (KR 10억 / US $10M / 코인 $50M) |
| 우선주·스팩·리츠 미필터 | 보통주만(코드 끝자리 0) + 이름 패턴 제외 |
| `pos_52w`가 실제로는 9개월 고저점 (데이터 270일) | 데이터 420일로 확대 + 시장별 연간 봉 수 기준으로 계산 |
| 벤치마크 조회 실패 시 조용히 0.0 반환 | 예외로 중단 |
| 종목 분석 실패를 조용히 삼킴 | 실패 건수·비율 로깅, 30% 초과 시 경고 |
| 16:00 KST 실행 시 코인 당일 봉 미완성 → 거래량 왜곡 | 진행 중인 봉 제거 + KRX 실행 시각 18:00으로 이동 |
| 코인 티커에 폐기된 MATIC 포함, 25개인데 "Top 30" 표기 | POL로 교체 + 30개로 보정 |

**C. 대시보드**

- **백테스트 페이지가 데이터를 전혀 못 읽던 버그 수정** — 업로드는 `{kr:{...}, us:{...}}`
  구조인데 `bt["common_trend"]`로 바로 접근해 전부 `undefined`였음. 마켓 선택 UI 추가
- Profit Factor / 손익비 / 손절 도달률 / 수익 중앙값 표시 추가
- 손실 0건일 때 PF를 `∞`로 표기 (기존엔 0.0으로 저장돼 최악 성과로 오독)
- 종목 테이블에 거래대금 컬럼 추가
- 생존 편향·표본 독립성 한계를 명시하는 해석 주의 문구 상시 노출

**변경 파일**
- `screener/market_config.py` — 데이터 기간·유동성·거래비용·임계값 설정 추가
- `screener/screener.py` — `get_universe()`, `score_pattern()`, `REQUIRED_FNS`, 거래가능성 판정 추가
- `screener/backtest.py` — 표본 스캔·비용·손절·벤치마크 반영으로 전면 재작성
- `screener/main.py` — 백테스트 호출부를 스크리너 결과와 분리
- `.github/workflows/screener.yml` — 실행 시각 18:00 KST, 타임아웃 90분
- `dashboard/app/backtest/page.tsx` — 마켓 선택 + 신규 지표 + 주의 문구
- `dashboard/components/StockTable.tsx`, `dashboard/lib/types.ts` — 거래대금 컬럼

**검증 결과** (KR 표본 250종목)

```
유니버스 필터   2763 → 1238종목 (우선주/스팩/시총)
신호 계산 성공  247 / 250
거래가능 필터   247 → 143종목 (거래대금 10억 미만·거래정지 제외)

패턴별 통과 수  기존(40점,게이트X) → 신규(60점,게이트O)
  p3      40 → 8      vcp     34 → 1
  p1      25 → 4      stage2  24 → 2
  wyckoff 22 → 2      p2      16 → 1
  darvas   3 → 0      canslim  2 → 1

common_trend / common_accum / common_all = 각 1종목
→ 전체 유니버스 환산 시 각 5종목 내외
```

---

## 시장별 특화 기법 정리

### 🇺🇸 미국 시장 특화

**현재 적용 중인 것 (8개 패턴 모두 적용, 특히 효과적인 것)**
- **CAN SLIM** — 원래 미국 시장용으로 개발. S&P500, NASDAQ 성장주에 가장 잘 맞음
- **Darvas Box** — 미국 1950~60년대 성장주 대상 개발. 지금도 나스닥 테크주에 유효
- **Stage 2** — NYSE/NASDAQ 대형주 사이클 분석에 최적화

**미국 전용 추가 가능 기법** *(현재 미구현)*
- **Earnings Surprise**: SEC EDGAR API로 EPS 어닝 서프라이즈 종목 필터
- **IBD RS Rating**: O'Neil의 상대강도 1~99 점수 (별도 계산 필요)
- **Cup and Handle**: 컵앤핸들 패턴 — 미국 성장주에서 가장 빈번한 돌파 패턴
- **Sector Rotation**: GICS 11개 섹터 강도 비교 — 돈이 어디로 흐르는지 추적

---

### ₿ 코인 특화

**현재 적용 중인 것 (임계값 조정 적용, 특히 효과적인 것)**
- **Wyckoff** — 코인 시장에서 가장 잘 먹힘. BTC 고래들이 Wyckoff 패턴 그대로 사용
- **VCP** — 코인 급등 전 변동성 수축 패턴. Solana, ETH 등에서 반복적으로 출현
- **볼린저 밴드 수축 + OBV** — 코인 매집 탐지에 특히 강력

**코인 전용 추가 가능 기법** *(현재 미구현)*
- **BTC 도미넌스 연동**: BTC 도미넌스 하락 시 알트코인 시즌 → 알트 강세 필터
- **펀딩비(Funding Rate)**: 선물 과열 여부 판단 (Binance API 필요)
- **온체인 지표**: OBV의 온체인 버전 — 고래 지갑 이동 추적 (Glassnode API 유료)
- **반감기 사이클**: BTC 4년 주기 — 반감기 후 12~18개월이 최대 강세장
- **Fear & Greed Index**: 극단적 공포 = 매수 기회 (alternative.me API 무료)
- **Elliott Wave**: 코인에서 파동이론이 주식보다 더 명확하게 발현

---

## 작업 예정

| 우선순위 | 작업 | 상태 |
|---------|------|------|
| 1 | Python 스크리너 기본 구조 | ✅ 완료 |
| 2 | Firebase Firestore 업로드 | ✅ 완료 |
| 3 | GitHub Actions 자동화 | ✅ 완료 |
| 4 | 8패턴 기술적 분석 스크리너 (전설 트레이더) | ✅ 완료 |
| 5 | 52주 신고가 + 상대강도(RS) + ATR 손절가 | ✅ 완료 |
| 6 | Next.js 대시보드 | ✅ 완료 |
| 7 | DART API EPS/매출 성장률 연동 | ✅ 완료 |
| 8 | 백테스트 모듈 (3개 공통 카테고리) | ✅ 완료 |
| 9 | 백테스트 대시보드 페이지 | ✅ 완료 |
| 10 | 멀티마켓 지원 (미장 + 코인) | ✅ 완료 |
| 11 | 백테스트 선택 편향 제거 + 거래비용/손절 반영 | ✅ 완료 |
| 12 | 필수조건 게이트 + 유동성/거래정지 필터 | ✅ 완료 |
| 13 | Vercel 배포 연동 | 대기 |
| 13b | 과거 재현 (리스트 단위 성과 측정) | ✅ 완료 |
| 14 | 기관/외인 순매수 — KR (pykrx 연동) | 🔜 예정 |
| 15 | 미국 EPS 어닝 서프라이즈 (SEC EDGAR API) | 🔜 예정 |
| 16 | 코인 BTC 도미넌스 연동 | 🔜 예정 |
| 17 | 코인 Fear & Greed Index 연동 (무료) | 🔜 예정 |
| 18 | 실시간 주가 연동 (KIS OpenAPI WebSocket) | 🔜 예정 |
| 19 | 분기 재무 히스토리(재고 포함) → fundamentals 컬렉션 | ✅ 완료(증분 미적용) |
| 20 | 실적 비율 계산 (ROE/ROA/마진/부채비율) | ✅ 완료 |
| 21 | ⭐ 재고 사이클 지표 + 8분기 추세 차트 | ✅ 완료 |
| 21b | PER/PBR/PSR 밸류에이션 + 리스트 표기 | ✅ 완료 |
| 21c | 패턴별 설명 모달 (? 버튼) | ✅ 완료 |
| 22 | 기업 비교 (KSIC 업종 내 밸류/마진) | ✅ 완료 |
| 23 | 공시 키워드 분석 (증설·수주·감산 등) | 🔜 예정 |
| 24 | ⭐ AI 투자 리포트 생성 | 🔜 예정 |
| 25 | 조건 알림 (재고 급감·수주 공시 등) | 🔜 예정 |

---

## 📊 펀더멘털 + 업황 사이클 분석 서비스 로드맵 (2026-08-03)

### 목적
DART 공시 데이터를 자동 수집하고, 재무 흐름·재고 사이클·산업 업황을 분석해
AI 투자 리포트를 생성하는 개인용 분석 서비스.

### 스택 (확정)
기존 구조 그대로 확장. **Spring/MariaDB/모바일 도입 안 함.**
```
GitHub Actions → Python (screener/) → Firebase → Next.js 웹 대시보드
외부 API: DART Open API, Claude/OpenAI API
```

### ⚠️ DART API 핵심 주의사항 (구현 전제)
1. **재고자산은 `finstate`에 없음** — 현재 dart_fetcher.py가 쓰는 `finstate`(주요계정)에는
   재고자산·매출채권이 안 나옴. 전체 재무제표 `finstate_all`(fnlttSinglAcntAll)을 써야 함.
2. **DART 손익 데이터는 누적(YTD)값** — 3Q 보고서 = 9개월 누적 매출.
   분기 단독값은 `당분기누적 − 전분기누적` 차감 계산 필요.
   재무상태표(재고·자산)는 시점값이라 그대로 사용, 손익(매출·이익)만 차감.
3. **계정명이 회사마다 다름** — 재고자산/제품/상품 등. account_nm 매칭은 실데이터로 검증.

### 기능 → 파일 매핑
| 기능 | 위치 | 비고 |
|------|------|------|
| ① 분기 재무 수집(재고 포함) | `dart_fetcher.py` 확장 | finstate_all + 최근 8분기 누적 저장 |
| ② 실적 비율 계산 | `analysis.py`(신규) | 성장성·수익성(ROE/ROA)·안정성(부채비율) |
| ③ 재고 사이클 | `analysis.py` | 재고증가율·재고부담·회전율. ①에 종속 |
| ④ 기업 비교 | Firebase + 대시보드 | 업종 내 PER/PBR/영업이익률 비교 |
| ⑤ 공시 키워드 | `disclosure.py`(신규) | DART 공시목록 API + 키워드 매칭 |
| ⑥ AI 리포트 | `ai_report.py`(신규) | Claude/GPT. **이 프로젝트의 본체** |
| ⑦ 조건 알림 | `main.py` + webhook | Discord/이메일 |

### MVP 단계 (①이 나머지를 여는 열쇠)
- **1단계**: ① 분기 재무 누적 저장 → ② 비율 계산 → ③ 대시보드 재무/재고 그래프
- **2단계**: ④ 기업 비교 → ⑤ 공시 키워드 수집
- **3단계**: ⑥ AI 리포트 → ⑦ 알림

## 2026-08-12 — 패턴 조합 실측: "겹치면 신뢰도↑" 가정 반박

`common_trend`("4개 중 2개 겹침")가 실제로 단독보다 나은지 처음으로 측정했다.

**결과**: 돌파 공통 20일 선정력 −0.20%(t=−0.20), Stage 2 단독 +1.92%.
겹침을 요구하면 오히려 희석된다. 26개 쌍 중 1위 `canslim+vcp`(+8.75%)는
최고 하루를 빼면 −0.33%인 꼬리 아티팩트였다.

**방법론 수정 3건** (첫 측정이 틀렸던 부분):
- t 통계 — 4개 부분표본 평균에 다시 √4를 나눠 SE가 절반이 되어 |t|가 2배 부풀었음.
  Newey-West 4랙으로 교체 (20일 보유 / 7일 스캔 = 4중 겹침).
- 종목 중앙값 기준선 — 유니버스 자체의 종목 중앙 초과수익(−3.24%)을 빼야 의미가 있음.
  개별 종목 수익률이 우측 치우침이라 어떤 부분집합이든 중앙 < 평균이기 때문.
- `common_trend` / `common_all` 게이트 자체를 측정 대상에 추가 (기존엔 28개 쌍만 쟀는데
  정작 화면에 뜨는 게이트를 안 쟀음). 화면 노출분인 점수 상위 20도 별도 측정.

**화면 스테일 텍스트 수정**: 홈·백테스트 화면이 여전히 "Stage2+CANSLIM+Darvas 중 2개"
(실제는 VCP 포함 4개), "Wyckoff + VCP 둘 다"(실제는 Wyckoff 단독)로 표기돼 있었다.

**동점 처리 누락 수정**: 상위 20 산출이 불안정 정렬의 행 순서에 의존했다 —
과거 Stage 2 가짜 +2.46%를 만든 버그와 동일. `TIE_SEED=7` 지터 적용.
동점 비율 0~5%로 낮아 결과는 −0.95% → −0.73%로 소폭 변동.

**5·20·60일 전 기간 측정 결과 대부분의 패턴이 기간에 따라 부호가 뒤집힌다.**
Stage 2는 20일 +1.92%지만 60일 −0.53%다 — 첫 보고에서 이 값을 대조군으로 쓴 것을 철회했다.
전 구간 일관된 것은 Darvas가 계속 음수라는 것과 CAN SLIM 종목중앙차가 계속 최악이라는 것뿐.

**게이트는 변경하지 않음** — 유의하지 않은 결과로 규칙을 바꾸면 과적합이다.

변경 파일: [screener/analyze_combos.py](screener/analyze_combos.py) (신규),
[screener/screener.py](screener/screener.py),
[dashboard/lib/patternGuide.ts](dashboard/lib/patternGuide.ts),
[dashboard/app/page.tsx](dashboard/app/page.tsx),
[dashboard/app/backtest/page.tsx](dashboard/app/backtest/page.tsx),
[검증기록.md](검증기록.md)

## 2026-08-12 — 가상 매매 (페이퍼 트레이딩)

패턴 목록에서 고른 종목을 담아두고 경과를 추적하는 화면.

**저장 위치를 localStorage로 한 이유**: 이 프로젝트에는 로그인이 없다. Firestore 규칙에서
쓰기를 열면 주소만 알면 누구나 포트폴리오를 지우거나 조작할 수 있다. 개인 기록이라
브라우저에 두는 편이 안전하다. 대가로 기기 간 이동이 안 되므로 JSON 내보내기/가져오기를 넣었다.

**`prices/{market}` 컬렉션 신설**: 패턴 목록에는 20종목만 담기므로 산 종목이 다음날
목록에서 빠지면 현재가를 알 수 없다. 스크리너가 이미 받아둔 유니버스 전 종목 시세를
그대로 올려 그 구멍을 막는다 (KR 2,600종목 ≈ 103KB, Firestore 1MB 한도 안).
`market_date`를 함께 저장해 화면에 기준일을 표시한다 — 실행이 실패해 문서가 묵으면
어제 값이 '현재가'로 조용히 표시되는 게 최악이라서.

기능: 종목 검색(코드·이름) 담기 · 패턴 태그 · 현재가 청산 · 청산 취소 · 손익/수익률/승패 합계
· 시세 없는 종목은 합계에서 제외하고 건수 표시 · JSON 백업

**외국인·기관 순매수는 미구현 (외부 차단)**: KRX가 투자자별 데이터에 로그인을 요구하도록
바뀌었다. pykrx 대량 API는 `KRX 로그인 실패`, 원시 엔드포인트는 `400 LOGOUT`을 반환한다.
data.krx.co.kr 무료 회원가입 후 `KRX_ID`/`KRX_PW`를 GitHub Secrets에 넣어야 열린다.
네이버 스크래핑은 조용히 깨지는 비공식 경로라 투자 화면에 쓰지 않는다.

변경 파일: [screener/screener.py](screener/screener.py) (run이 names도 반환),
[screener/main.py](screener/main.py), [screener/firebase_upload.py](screener/firebase_upload.py) (save_prices),
[dashboard/lib/portfolio.ts](dashboard/lib/portfolio.ts) (신규),
[dashboard/app/portfolio/page.tsx](dashboard/app/portfolio/page.tsx) (신규),
[dashboard/lib/types.ts](dashboard/lib/types.ts), [dashboard/lib/fetcher.ts](dashboard/lib/fetcher.ts),
[dashboard/components/Header.tsx](dashboard/components/Header.tsx)

**보정 3건** (첫 구현의 실수):
- 진입일이 UTC 기준이라 KST 오전 0~9시에 하루 밀려 저장됐다. `toLocaleDateString("sv-SE")`로 교체.
- 시세 기준일을 `datetime.today()`로 찍었는데 08:30 KST 크론이 UTC로는 전날 23:30이라
  실제 데이터 날짜와 어긋난다(월요일 아침은 이틀). 시계 대신 데이터의 마지막 봉 날짜를 쓴다
  (`calc_signals_from_df`가 `bar_date`를 반환 → `screener.run`이 최빈값을 meta로 넘김).
- 수익률이 세전이라 성적표·3년치 통계(비용 반영)와 나란히 놓으면 잘못 비교하게 된다.
  `market_config.py`와 같은 왕복 비용(KR 0.41%)을 적용하고 화면에 표기했다.

**패턴별 내 성적 집계 추가** — 성적표가 '화면에 떴던 종목 전체'를 재는 것과 달리
여기는 '내가 실제로 고른 것'만 잰다.

**Firestore 규칙에 `prices` 읽기 추가 필요** — 안 하면 화면이 빈 상태로 뜬다.

## 2026-08-12 — 외국인·기관 순매수 (KR 전용)

CAN SLIM의 I(기관 수급). 가격·거래량에서 파생되지 않은 유일한 독립 축이다.

**KRX 로그인 필요**: 투자자별 데이터는 이제 인증을 요구한다(비로그인 시 `400 LOGOUT`).
pykrx가 `KRX_ID`/`KRX_PW` 환경변수로 로그인 전체를 처리하므로 별도 로그인 코드는 없다.
자격증명이 없으면 조용히 건너뛴다 — 수급 하나 때문에 스크리닝 전체가 죽으면 안 된다.

**시총 대비 비율(%)로 정규화**: 원화 절대액은 대형주가 항상 이겨 종목 간 비교가 안 된다.
20일·60일 두 구간을 본다 (O'Neil은 기관이 여러 분기에 걸쳐 늘리는 것을 본다).

**점수에는 넣지 않았다** — 표시 컬럼으로만 붙인다. `common_trend`의 "겹치면 낫다"가
실측에서 무너진 전례가 있어, 몇 달 쌓아 실측한 뒤에 게이트를 논한다.

검증: `python investor_flow.py` (screener/.env 의 KRX_ID/KRX_PW를 읽는다)

변경 파일: [screener/investor_flow.py](screener/investor_flow.py) (신규),
[screener/main.py](screener/main.py), [screener/requirements.txt](screener/requirements.txt),
[.github/workflows/screener.yml](.github/workflows/screener.yml) (KRX 시크릿 전달),
[dashboard/components/StockTable.tsx](dashboard/components/StockTable.tsx) (외인·기관 토글),
[dashboard/lib/types.ts](dashboard/lib/types.ts)

**SNS 가입 계정은 그대로는 안 된다** (2025-12-27 KRX 회원제 전환).
네이버·카카오 간편가입은 OAuth 리다이렉트(`/contents/MDC/COMS/social/auth.cmd`)를 타는데
pykrx는 `mbrId`+`pw` POST만 지원한다. KRX 로그인 페이지 안내:
> SNS 계정으로 가입하신 경우, 아이디/비밀번호 로그인을 이용하시려면
> '마이페이지 > 정보수정'에서 비밀번호를 신규 설정해 주세요.

`check_login()`이 KRX 응답 코드를 그대로 노출한다 — pykrx는 성공 여부만 보고 사유를
삼켜서 실패하면 "조회 결과 없음"만 남는다. CD001 정상 / CD005 아이디 미존재(=SNS 가입) /
CD010 비밀번호 변경 필요 / CD011 중복 로그인.

⚠️ 실제 조회 응답으로는 아직 검증하지 못했다 — 유효한 자격증명 필요.

## 2026-08-14 — 종목 조회: 아무 종목이나 검색해 패턴별 점수·탈락 사유 확인

패턴 목록에는 상위 20종목만 담긴다. "내가 보는 이 종목은 왜 없지?"에 답할 방법이 없었다.

**점수 0점은 세 가지를 구분 못 한다** — 대상 제외(거래정지·거래대금 미달) / 게이트 탈락 /
게이트는 통과했는데 점수 미달. 화면에서 셋이 같아 보이면 아무것도 알 수 없어 상태를 나눠 저장한다.
탈락 조건은 이름만이 아니라 **실측값과 기준을 함께** 보여준다 (`RS Rating 54 / 기준 70 이상`).

**게이트 진실은 한 곳**: 판정은 `screener.REQUIRED_FNS`가 하고, `explain.GATE_CONDITIONS`는
설명 전용이다. 둘이 어긋나면 화면이 "이 조건 때문"이라 하는데 실제 판정은 다른 이유가 된다 —
`python explain.py kr`이 패널 68,417행 × 8패턴 = 547,336건으로 동치를 검사한다(현재 전부 일치).

**샤드 8개**: 전 종목 × 11패턴 × 조건 실측값은 2.2MB로 Firestore 문서 한도(1MB)를 넘는다.
조건 라벨은 종목마다 같으므로 인덱스 문서에 한 번만 두고 종목별로는 통과여부+실측값만 담아
샤드당 약 100KB. 화면은 검색한 종목이 속한 샤드 하나만 읽는다.
샤드 키는 `sum(ord(c)) % 8` — 한국 종목코드는 끝자리가 전부 0이라 마지막 글자로 나누면
한 샤드에 2,111종목이 전부 몰린다(실측 확인). 현재 분포 편차 1.09.

**numpy 타입 함정**: `DataFrame.to_dict`는 `np.int64`를 주는데 `int`의 하위형이 아니라
`isinstance` 검사가 조용히 0으로 떨어뜨린다. 숫자 변환을 `float()` 시도 방식으로 바꿨다.

검색은 기존 `prices/{market}.names`를 재사용한다 — 검색 인덱스를 따로 만들지 않는다.

변경 파일: [screener/explain.py](screener/explain.py) (신규),
[screener/screener.py](screener/screener.py) (run이 전 종목 신호를 meta로 반환),
[screener/main.py](screener/main.py), [screener/firebase_upload.py](screener/firebase_upload.py) (save_signals),
[dashboard/app/lookup/page.tsx](dashboard/app/lookup/page.tsx) (신규),
[dashboard/lib/fetcher.ts](dashboard/lib/fetcher.ts) (shardOf — 파이썬과 같은 식),
[dashboard/lib/types.ts](dashboard/lib/types.ts), [dashboard/components/Header.tsx](dashboard/components/Header.tsx)

**Firestore 규칙에 `signals` 읽기 추가 필요** (`prices`와 함께).

## 2026-08-19 — KRX IP 차단으로 KR 스크리닝 전체 실패 → 미러 경로로 우회

GitHub Actions 실행이 `get_universe`에서 죽었다.
`ValueError: Failed to load data from http://data.krx.co.kr/comm/bldAttendant/executeForResourceBundle.cmd`
응답 본문은 Akamai의 `Access Denied` HTML (`errors.edgesuite.net`).

**진단**: 로컬에서는 `fdr.StockListing("KOSPI")`가 정상 동작한다. KRX가 데이터센터 IP를
엣지에서 차단한 것이지 로그인 문제가 아니다(로그인 이전 단계에서 403).

**핵심**: `fdr.StockListing`은 실제 CSV를 이미 GitHub 미러에서 받는다. KRX에는
"최신 영업일이 언제냐"를 묻는 요청 하나만 보내는데 그게 막혀 전체가 죽었다.
날짜를 오늘부터 거꾸로 최대 14일 훑어 미러에서 직접 찾으면 KRX를 아예 안 거친다.
직접 조회와 미러 결과가 동일함을 확인했다 (KOSPI 942 / KOSDAQ 1821, 차집합 0, 컬럼 동일).

**영향 없는 것**: 개별 종목 OHLCV는 네이버(`NaverDailyReader`), 지수는 FDR 캐시라 무관하다.
미국(Wikipedia)·코인도 무관. 이번 실행에서 `KS11` 벤치마크가 성공한 게 그 증거였다.

**영원히 막히는 것**: 외국인·기관 순매수. Akamai가 로그인 이전 단계에서 막으므로
`KRX_ID`/`KRX_PW`를 뭘 넣어도 GitHub Actions에서는 안 된다. 에러 메시지가 이를 명시하도록
고쳤다 — 자격증명 문제로 오해하면 Secrets만 계속 고치게 된다. 로컬 실행에서는 동작한다.

변경 파일: [screener/screener.py](screener/screener.py) (krx_listing),
[screener/investor_flow.py](screener/investor_flow.py) (IP 차단 메시지 구분)

## 2026-08-19 — 종목 조회가 빈 화면인 원인: Firestore 중첩 배열 거부

규칙은 게시돼 있고(`PERMISSION_DENIED` 아님) 같은 날 실행도 성공해 `prices/kr`은
들어갔는데 `signals`만 문서가 0개였다.

**원인**: Firestore는 배열 요소로 배열을 허용하지 않는다. 판정 데이터를
`[상태, 점수, [[통과, 실측값], ...]]`로 담아 문서 전체가 거부됐다.
`main.py`의 `try/except`가 예외를 삼켜 로그 한 줄만 남고 화면에는 "데이터 없음"으로 보였다.

**수정 3건**:
- 조건을 맵의 배열 `{"s":상태, "v":점수, "c":[{"o":통과, "d":실측값}]}`로 변경 (배열 안의 맵은 허용)
- `_reject_nested_arrays()` — 업로드 직전 검사. 조용히 거부되는 게 최악이라 예외로 세운다
- 실패 시 traceback을 찍는다. 한 줄 메시지는 로그에서 놓친다
- 샤드를 배치로 묶지 않고 개별 저장 (샤드 하나가 150KB대)

변경 파일: [screener/explain.py](screener/explain.py),
[screener/firebase_upload.py](screener/firebase_upload.py), [screener/main.py](screener/main.py),
[dashboard/lib/types.ts](dashboard/lib/types.ts), [dashboard/app/lookup/page.tsx](dashboard/app/lookup/page.tsx)

## 2026-08-19 — 성적표 수익률이 액면분할·무상증자를 반영 못 한 버그

제보: 진입일 08-04 CAN SLIM의 티앤엘 진입가 63,500이 실제 가격이 아니었다.

**원인**: 저장된 진입가 vs 오늘 시세를 비교했는데, 그 사이 자본 조정이 있으면 FDR이
과거 시계열을 소급 조정하므로 조정 전 값과 조정 후 값을 비교하게 된다.

**전수 조사**: 9,606건 중 244건(2.5%) / 77종목이 영향. 오차 중앙값 8.9%p, 최대 68.9%p.
티앤엘 −47.0% → 실제 **+17.9%** (부호 반전), 가온전선 −49.6% → −12.2%.
배율이 정수배로 깔끔한 것이 증거 — 미래산업 5.000 / 동양 0.500 / 티앤엘 1.974.

**수정**: 스크리너가 실행 중 종가 이력(최근 90봉)을 모아 성적표에 넘긴다. 성적표는
같은 시점의 한 시계열에서 진입일과 오늘 종가를 모두 읽는다. 자본 조정이 구조적으로 반영된다.
저장값과 2% 이상 벌어지면 `adjusted` 표시 — 점수·RSI 등은 조정 전 기준이라 밝힌다.

**같이 수정**: `screener_results`에 시장별 `{market}_bar_date` 저장. `market_date`가
UTC 시계라 08:30 KST 실행에서 실제 봉 날짜와 어긋났고, 라벨이 틀리면 종가 조회도 틀어진다.

**가상 매매**: 손으로 넣은 매수가는 보정 불가 — 현재가와 정수배에 가까우면 "분할?" 표시로
확인만 요청한다. 사용자가 입력한 값을 말없이 고쳐 쓰지 않는다.

변경 파일: [screener/screener.py](screener/screener.py) (종가 이력 수집),
[screener/tracker.py](screener/tracker.py), [screener/firebase_upload.py](screener/firebase_upload.py),
[screener/main.py](screener/main.py), [dashboard/app/track/page.tsx](dashboard/app/track/page.tsx),
[dashboard/lib/portfolio.ts](dashboard/lib/portfolio.ts),
[dashboard/app/portfolio/page.tsx](dashboard/app/portfolio/page.tsx),
[dashboard/lib/types.ts](dashboard/lib/types.ts), [검증기록.md](검증기록.md)

## 2026-08-20 — 종목별 수급·공시 분석 패널

검색하거나 패턴 목록에서 종목을 누르면 그 종목의 외국인·기관 수급과 공시가 함께 뜬다.
`SupplyPanel`을 `/lookup`과 `StockDetailModal` 양쪽에서 재사용한다.

**공시(DART)** — KRX와 무관한 서버라 GitHub Actions에서도 받는다.
종목당 수백 건이 오므로(삼성전자 180일 908건) 제목으로 걸러 20건 이내만 남긴다.
생략 건수를 함께 저장해 "다 보여준 척"하지 않는다.

- **자본 조정**(무상증자·분할·권리락·감자)은 별도 경고 블록으로 맨 위에 띄운다.
  주가가 기계적으로 바뀌어 과거 가격과의 비교 자체가 무너지기 때문이다.
  실제로 티앤엘 `2026-08-05 권리락(무상증자)`이 잡힌다 — 성적표를 −47%로 만든 그 사건이다.
- 그 외: 유상증자·전환사채·잠정실적·공급계약·자기주식·최대주주·상장폐지 등
- `[기재정정]` 중복은 같은 날 같은 제목 기준으로 최신 하나만 남긴다

**수급(KRX)** — `flows/{market}`에서 읽는다. 로컬 `enrich_local.py`로만 채워지므로
없으면 그 사실을 화면에 밝힌다. 외국인·기관 20일/60일 순매수(시총 대비 %),
공매도 잔고·거래 비중, PER/PBR/배당.

**점수 게이트에는 넣지 않았다.** 선정력이 실측되지 않았고, `common_trend`의 "겹치면 낫다"가
실측에서 무너진 전례가 있다. 화면에도 그렇게 표기했다.

**일별 수급 시계열은 보류** — 종목당 KRX 호출이 필요해 로컬에서 미리 받은 종목만 가능하다.
검색은 아무 종목이나 되므로 가용성이 달라져 섞으면 화면 상태가 세 가지가 된다.

변경 파일: [screener/dart_fetcher.py](screener/dart_fetcher.py) (fetch_disclosures),
[screener/main.py](screener/main.py), [screener/firebase_upload.py](screener/firebase_upload.py),
[dashboard/components/SupplyPanel.tsx](dashboard/components/SupplyPanel.tsx) (신규),
[dashboard/components/StockDetailModal.tsx](dashboard/components/StockDetailModal.tsx),
[dashboard/app/lookup/page.tsx](dashboard/app/lookup/page.tsx),
[dashboard/lib/fetcher.ts](dashboard/lib/fetcher.ts), [dashboard/lib/types.ts](dashboard/lib/types.ts)

**Firestore 규칙에 `flows`·`disclosures` 읽기 추가 필요.**

## 2026-08-20 — 시장 폭 표시 (레짐 게이트는 측정 후 보류)

원전 기법이 0~3종목인 하락장에서 자체 패턴이 30종목씩 내는 문제. 게이트를 넣기 전에 측정했다.

- `market_uptrend`는 63스캔 중 85%가 참이라 게이트로 못 쓴다
- **시장 폭**(150일선 위 비율)이 잘 가른다 — p1 +3.16%p, p2 +3.61%p 차이
- 단 **p3는 오히려 약한 장에서 나았고**(−0.44%p), Stage 2의 효과는 2025-04 두 날에 의존
- 일괄 게이트는 p3를 막고, 패턴별 게이트는 9일에 맞추는 과적합

→ **차단하지 않고 표시.** 시장 폭을 계산해 대시보드 상단에 띄우고, 50% 미만이면
자체 패턴의 실측 이력을 경고로 함께 보여준다. 점수·게이트 미변경.

홈 화면 그룹 설명에 남아 있던 "신뢰도 최상"도 "겹침 이득은 실측 안 됨"으로 고쳤다.

변경 파일: [screener/screener.py](screener/screener.py) (breadth 계산),
[screener/firebase_upload.py](screener/firebase_upload.py), [screener/main.py](screener/main.py),
[dashboard/app/page.tsx](dashboard/app/page.tsx) (BreadthBar), [dashboard/lib/types.ts](dashboard/lib/types.ts),
[검증기록.md](검증기록.md)

## 2026-08-20 — 원전 진입 기준점 표시

제보: 슈프리마가 VCP에 오래 떠 있는데 분석해보니 **진입점이 이미 지나 있었다**
(피벗 49,550, 현재가 +16%, 원전 손절까지 −23%).

측정 결과 전체 패턴 특성이었다 — VCP 통과 935건의 피벗 대비 중앙값 +21.7%,
O'Neil 매수 구간(5% 이내)은 19%뿐. 추격 구간별 선정력은 +0.99% / +0.46% / −0.23%로
단조 감소하지만 t가 모두 1 미만이라 확정 불가.

**대응**: 판정하지 않고 사실만 표시. 패턴 카드에 원전 진입 기준점·현재가와의 거리·
원전 손절가·손절까지 거리를 띄우고, 실측 수치를 함께 밝힌다.

- `vcp_stop` 신규 (= 피벗 × (1−마지막 수축 깊이), Minervini 손절)
- `vcp_pivot_date` 신규 — 피벗이 재설정되므로(46,800 → 49,550) 기준일을 밝혀야 한다
- `signals` 샤드도 JSON 문자열로 전환 — 필드가 늘어 색인 한도(4만)에 걸릴 위험

변경 파일: [screener/patterns.py](screener/patterns.py), [screener/screener.py](screener/screener.py),
[screener/explain.py](screener/explain.py), [screener/firebase_upload.py](screener/firebase_upload.py),
[dashboard/app/lookup/page.tsx](dashboard/app/lookup/page.tsx),
[dashboard/lib/types.ts](dashboard/lib/types.ts), [dashboard/lib/fetcher.ts](dashboard/lib/fetcher.ts),
[검증기록.md](검증기록.md)

## 2026-08-20 — 자동 실행을 하루 3회로

08:30 / 15:40 / 20:00 KST (UTC 크론 `30 23 * * 0-4`, `40 6 * * 1-5`, `0 11 * * 1-5`).

- **09시대는 넣지 않았다.** 장중이라 당일 봉이 미완성이고 `vol_ratio`가 0에 가까워져
  `vol_ratio >= 2`를 요구하는 CAN SLIM의 S와 Darvas가 전부 탈락한다.
- **15:40은 일봉 미확정 가능성이 있다** — 종전 18:00을 쓴 이유가 그것이었다.
  같은 날 20:00 실행이 덮어쓰므로 최종값은 확정본이 된다.
- 넥스트레이드(20시 마감) 거래분이 우리 일봉에 포함되는지는 **아직 미확인.**
  FDR과 pykrx가 같은 숫자를 주는 것만 확인했고, 둘 다 KRX 기준일 수도 있어 갈라지지 않는다.
  장중 스냅샷을 찍어뒀으니 15:40 / 20:00 이후 거래량 변화로 판별 가능하다.

변경 파일: [.github/workflows/screener.yml](.github/workflows/screener.yml),
[Architecture.md](Architecture.md)

## 2026-08-20 — 진입 기준점이 한 번도 업로드되지 않던 버그

`explain.pack()`이 `entry_info()`를 호출하는데 **그 함수가 존재하지 않았다.** b585648에서
호출부와 화면만 들어가고 함수 본문이 빠졌다. 첫 종목 첫 패턴에서 `NameError`가 나
`build_shards()`가 통째로 죽었고, [main.py:151](screener/main.py#L151)의 `except`가 이를 삼켰다.

**영향이 진입점 표시에 그치지 않는다.** `save_signals`가 아예 실행되지 않았으므로
`signals/kr_*` 문서가 **b585648 이후 한 번도 갱신되지 않았다.** `/lookup`의 점수·조건·탈락 사유·
기준일이 전부 그 시점 값이다. 화면에 기준일이 표시되므로 확인 가능하다
([lookup/page.tsx:150](dashboard/app/lookup/page.tsx#L150)).

같은 자리에 두 번째 버그가 있었다 — `e = entry_info(...)`가 `e = explain(...)` 결과를 덮어써서,
함수를 정의해도 두 번째 패턴에서 `TypeError`가 났다. `ex`로 분리.

- `ENTRY_REF` 신규 — 기준점·손절·기준일 필드와 라벨. [검증기록.md](검증기록.md) 근접도 실측이
  쓴 기준점(VCP 피벗 / Darvas 박스 천장 / Stage2·Wyckoff 거래범위 상단)과 대조해 일치 확인
- `verify()`가 `pack()`을 밟도록 확장. 게이트 동치만 검사해서 이 실패가 통과했다
- 실측: 통과 건 전부 기준점 적재 (VCP 935 / Darvas 1,176 / Stage2 2,472 / Wyckoff 3,611 = 100%).
  전체 행 기준 적재율은 18~19% — 박스·거래범위가 형성되지 않은 종목이 많아 정상

- **기준점이 있는 패턴은 8개 중 4개뿐이다.** p1~p3는 자체 패턴이라 대조할 원전이 없고,
  CAN SLIM은 O'Neil의 베이스 피벗이 원전에 있으나 미구현이다 — 화면에서는 둘 다 똑같이 빈다
- 업로드 실패가 워크플로를 빨갛게 만들도록 변경. 지금까지는 로그에만 남아 며칠간 모를 수 있었다.
  나머지 업로드를 다 끝낸 뒤 마지막에 종료 코드 1을 낸다 — 즉시 죽으면 백테스트까지 잃는다

변경 파일: [screener/explain.py](screener/explain.py), [screener/main.py](screener/main.py),
[검증기록.md](검증기록.md)
